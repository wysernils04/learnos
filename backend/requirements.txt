# Core
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Database
asyncpg>=0.29.0
pgvector>=0.3.0

# Auth / Security
PyJWT>=2.8.0
cryptography>=42.0.0

# HTTP client (SBB proxy, Supabase)
httpx>=0.27.0
supabase>=2.5.0

# File upload
python-multipart>=0.0.9

# PDF processing
pdfplumber>=0.11.0

# Embeddings — heavy; comment out to speed up Railway builds until Phase 3
sentence-transformers>=3.0.0

# LLM
anthropic>=0.28.0

# Audio transcription — very heavy (pulls PyTorch); enable in Phase 4
# openai-whisper>=20231117
